
const imagePath: string = "assets/images/";



export class ImageAssets {
    public logoImg: string = `${imagePath}iti-logo.png`;
}


const imageJsonPath = `${imagePath}json/`;

export class JsonAssets {
    public loader = `${imageJsonPath}loader.json`;
}

const imageSvgPath = `${imagePath}svg/`;

export class SvgAssets {
    public noData = `${imageSvgPath}no_data.svg`;
}
